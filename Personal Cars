task.wait(2)

-- Cleanup old Rayfield
for _, v in ipairs(game:GetService("CoreGui"):GetChildren()) do
    if v.Name == "Rayfield" then v:Destroy() end
end

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "😎 BUB_Scripts",
    LoadingTitle = "BUB_Scripts",
    LoadingSubtitle = "Auto Car Counter + Seat Lock",
    Theme = "Default",
    ToggleUIKeybind = "K",
    DisableRayfieldPrompts = true,
    DisableBuildWarnings = true,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "BUBScripts",
        FileName = "Settings"
    },
    KeySystem = true,
    KeySettings = {
        Title = "BUB_Scripts Key System",
        Subtitle = "Verification Required",
        Note = "Join the Discord to get the key: https://discord.gg/J9BKhsn4",
        FileName = "BUBScripts_Key",
        SaveKey = false,
        GrabKeyFromSite = true,
        Key = "https://pastebin.com/raw/8dPafPL1"
    }
})

local MainTab = Window:CreateTab("Main", 4483362458)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local RESET_CENTER = Vector3.new(8.582, -12.629, -34.327)
local RANDOM_RADIUS = 500
local MAX_DISTANCE = 3000
local TELEPORT_OFFSET = CFrame.new(0, 2, 0)

local isSpamming = false
local counterConnection = nil
local currentOwner = nil
local seatCache = {}

-- ==================== SEAT CACHE ====================
local function refreshSeatCache()
    seatCache = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("VehicleSeat") or obj:IsA("Seat") then
            table.insert(seatCache, obj)
        end
    end
end

refreshSeatCache()
workspace.DescendantAdded:Connect(refreshSeatCache)
workspace.DescendantRemoving:Connect(function(obj)
    for i = #seatCache, 1, -1 do
        if seatCache[i] == obj then table.remove(seatCache, i) break end
    end
end)

-- ==================== AUTO CAR COUNTER (Fast but lightweight) ====================
local function isProperlySeated()
    local hum = player.Character and player.Character:FindFirstChild("Humanoid")
    return hum and hum.SeatPart and hum.SeatPart.Occupant == hum
end

local function getNearestSeat()
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    local closest, dist = nil, math.huge
    for _, seat in ipairs(seatCache) do
        if seat and seat.Parent and not seat.Occupant then
            local d = (seat.Position - root.Position).Magnitude
            if d < dist and d <= MAX_DISTANCE then
                dist, closest = d, seat
            end
        end
    end
    return closest
end

local function teleportToSeat(seat)
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root or not seat then return end
    root.CFrame = seat.CFrame * TELEPORT_OFFSET
    task.wait(0.05)
    local hum = player.Character:FindFirstChild("Humanoid")
    if hum and not seat.Occupant then seat:Sit(hum) end
end

local function resetPosition()
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if root then
        local angle = math.random() * math.pi * 2
        local dist = math.random() * RANDOM_RADIUS
        root.CFrame = CFrame.new(RESET_CENTER + Vector3.new(math.cos(angle)*dist, math.random(-25,25), math.sin(angle)*dist))
    end
end

local function startAutoCounter()
    if counterConnection then return end
    isSpamming = true

    counterConnection = RunService.Heartbeat:Connect(function()
        if not isSpamming then return end
        if not isProperlySeated() then
            local nearest = getNearestSeat()
            if nearest then teleportToSeat(nearest) end
            resetPosition()
        end
    end)
end

local function stopAutoCounter()
    isSpamming = false
    if counterConnection then
        counterConnection:Disconnect()
        counterConnection = nil
    end
end

-- ==================== SEAT LOCK + EJECT (Separate & Efficient) ====================
local function ejectOccupant(seat)
    if seat and seat.Occupant then
        local hum = seat.Occupant
        hum.Jump = true
        task.wait(0.15)
        hum.Jump = false
    end
end

-- Main efficient check using Changed signal + slow backup
local function setupSeatLock()
    -- Primary: Only runs when someone sits/leaves
    for _, seat in ipairs(seatCache) do
        seat:GetPropertyChangedSignal("Occupant"):Connect(function()
            if not seat.Occupant then return end

            local occupantPlayer = Players:GetPlayerFromCharacter(seat.Occupant.Parent)
            if not occupantPlayer then return end

            if not currentOwner and occupantPlayer == player then
                currentOwner = player
                Rayfield:Notify({Title = "Seat Locked", Content = "You are now the owner", Duration = 3})
            elseif occupantPlayer ~= player and currentOwner == player then
                ejectOccupant(seat)
            end
        end)
    end

    -- Backup check (only 4 times per second)
    task.spawn(function()
        while true do
            if currentOwner == player then
                for _, seat in ipairs(seatCache) do
                    if seat.Occupant then
                        local occupantPlayer = Players:GetPlayerFromCharacter(seat.Occupant.Parent)
                        if occupantPlayer and occupantPlayer ~= player then
                            ejectOccupant(seat)
                        end
                    end
                end
            end
            task.wait(0.25)  -- 4 times per second
        end
    end)
end

-- Manual eject
player.Chatted:Connect(function(msg)
    if msg:lower() == "/eject" and currentOwner == player then
        local seat = player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.SeatPart
        if seat then
            ejectOccupant(seat)
            Rayfield:Notify({Title = "Eject", Content = "Everyone ejected from your seat", Duration = 3})
        end
    end
end)

-- ==================== UI ====================
MainTab:CreateToggle({
    Name = "Auto Car Counter",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            startAutoCounter()
            Rayfield:Notify({Title = "Auto Counter", Content = "ENABLED", Duration = 4})
        else
            stopAutoCounter()
            Rayfield:Notify({Title = "Auto Counter", Content = "DISABLED", Duration = 4})
        end
    end,
})

MainTab:CreateToggle({
    Name = "Seat Lock (Owner Only)",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            currentOwner = nil  -- reset owner
            setupSeatLock()
            Rayfield:Notify({Title = "Seat Lock", Content = "ENABLED - Sit first to claim", Duration = 4})
        else
            currentOwner = nil
            Rayfield:Notify({Title = "Seat Lock", Content = "DISABLED", Duration = 4})
        end
    end,
})

Rayfield:Notify({
    Title = "BUB_Scripts Loaded",
    Content = "Separated Counter + Seat Lock",
    Duration = 5,
})

print("✅ Optimized BUB_Scripts loaded - Counter and Seat Lock are now separate")
