task.wait(3)

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "😎 BUB_Scripts",
    LoadingTitle = "BUB_Scripts",
    LoadingSubtitle = "AutoCounter + NumKeyBindBot + Anti-Ragdoll + AutoClicker",
    Theme = "Default",
    ToggleUIKeybind = "K",
    DisableRayfieldPrompts = true,
    DisableBuildWarnings = true,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "BUBScripts",
        FileName = "Settings"
    },
    KeySystem = true,
    KeySettings = {
        Title = "BUB_Scripts Key System",
        Subtitle = "Verification Required",
        Note = "Join the Discord to get the key: https://discord.gg/J9BKhsn4",
        FileName = "BUBScripts_Key",
        SaveKey = false,
        GrabKeyFromSite = true,
        Key = "https://pastebin.com/raw/8dPafPL1"
    }
})

local MainTab = Window:CreateTab("Main", 4483362458)

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer

-- ==================== AUTO CLICKER ====================
local autoClickerEnabled = false
local clickConnection = nil

local function startAutoClicker()
    if clickConnection then return end
    clickConnection = RunService.Heartbeat:Connect(function()
        if not autoClickerEnabled then return end
        
        local character = player.Character
        if not character then return end
        
        local tool = character:FindFirstChildOfClass("Tool")
        if tool then
            VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
            task.wait(0.007)
            VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
        end
    end)
end

local function stopAutoClicker()
    if clickConnection then
        clickConnection:Disconnect()
        clickConnection = nil
    end
end

local function toggleAutoClicker()
    autoClickerEnabled = not autoClickerEnabled
    if autoClickerEnabled then
        startAutoClicker()
    else
        stopAutoClicker()
    end
end

-- L Key Toggle
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.L then
        toggleAutoClicker()
        Rayfield:Notify({Title = "Auto Clicker", Content = autoClickerEnabled and "✅ ENABLED" or "❌ DISABLED", Duration = 1.5})
    end
end)

-- ==================== AUTO CAR COUNTER ====================
local RESET_CENTER = Vector3.new(8.582, -12.629, -34.327)
local RANDOM_RADIUS = 500
local MAX_DISTANCE = 3000
local TELEPORT_OFFSET = CFrame.new(0, 2, 0)

local isSpamming = false
local seatConnection = nil
local seatCache = {}

-- (All your seat cache, getNearestSeat, teleportToSeat, resetPosition, startSeatSpam, stopSeatSpam functions stay the same)
local function refreshSeatCache()
    seatCache = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("VehicleSeat") or obj:IsA("Seat") then
            table.insert(seatCache, obj)
        end
    end
end

workspace.DescendantAdded:Connect(function(obj)
    if obj:IsA("VehicleSeat") or obj:IsA("Seat") then table.insert(seatCache, obj) end
end)

workspace.DescendantRemoving:Connect(function(obj)
    for i = #seatCache, 1, -1 do
        if seatCache[i] == obj then table.remove(seatCache, i) break end
    end
end)

local function getNearestSeat()
    local closest, dist = nil, math.huge
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    for _, seat in ipairs(seatCache) do
        if seat and seat.Parent and not seat.Occupant then
            local d = (seat.Position - root.Position).Magnitude
            if d < dist and d <= MAX_DISTANCE then
                dist = d
                closest = seat
            end
        end
    end
    return closest
end

local function teleportToSeat(seat)
    if not seat or not player.Character then return end
    local root = player.Character:FindFirstChild("HumanoidRootPart")
    if root then root.CFrame = seat.CFrame * TELEPORT_OFFSET end
    task.wait(0.03)
    local hum = player.Character:FindFirstChild("Humanoid")
    if hum and not seat.Occupant then seat:Sit(hum) end
end

local function getRandomPositionAround(center, radius)
    local angle = math.random() * math.pi * 2
    local dist = math.random() * radius
    return center + Vector3.new(math.cos(angle)*dist, math.random(-25,25), math.sin(angle)*dist)
end

local function resetPosition()
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if root then root.CFrame = CFrame.new(getRandomPositionAround(RESET_CENTER, RANDOM_RADIUS)) end
end

local function startSeatSpam()
    if seatConnection then return end
    refreshSeatCache()
    isSpamming = true
    seatConnection = RunService.RenderStepped:Connect(function()
        if not isSpamming then return end
        local nearest = getNearestSeat()
        if nearest then teleportToSeat(nearest) end
        resetPosition()
        task.wait(0.001)
    end)
end

local function stopSeatSpam()
    isSpamming = false
    if seatConnection then seatConnection:Disconnect() seatConnection = nil end
end

-- Death Handler
local function setupCharacter(character)
    character:WaitForChild("Humanoid").Died:Connect(function()
        task.wait(2)
        if isSpamming then
            task.wait(0.5)
            startSeatSpam()
        end
    end)
end

if player.Character then setupCharacter(player.Character) end
player.CharacterAdded:Connect(setupCharacter)

-- ==================== UI ====================
MainTab:CreateToggle({
    Name = "Auto Car Counter",
    CurrentValue = false,
    Callback = function(Value)
        if Value then startSeatSpam() else stopSeatSpam() end
    end,
})

MainTab:CreateToggle({
    Name = "Auto Clicker",
    CurrentValue = false,
    Callback = function(Value)
        autoClickerEnabled = Value
        if Value then startAutoClicker() else stopAutoClicker() end
    end,
    Info = "PC - Press L to enable/disable clicks"
})

MainTab:CreateSlider({
    Name = "Click Speed (Clicks/sec)",
    Range = {1, 60},
    Increment = 1,
    CurrentValue = 12,
    Callback = function() end,
})

MainTab:CreateToggle({
    Name = "Anti-Ragdoll",
    CurrentValue = false,
    Callback = function(Value)
        -- ←←← PUT YOUR ANTI-RAGDOLL CODE HERE ←←←
        Rayfield:Notify({Title = "Anti-Ragdoll", Content = Value and "ENABLED" or "DISABLED", Duration = 3})
    end,
})

MainTab:CreateToggle({
    Name = "0-9 Numpad Cycle",
    CurrentValue = false,
    Callback = function(Value)
        -- Put your numpad code here
        Rayfield:Notify({Title = "Numpad Cycle", Content = Value and "ENABLED" or "DISABLED", Duration = 3})
    end,
})

Rayfield:Notify({
    Title = "BUB_Scripts Loaded",
    Content = "K = Menu | L = Toggle Auto Clicker",
    Duration = 8,
})
