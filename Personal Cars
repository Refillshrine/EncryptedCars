task.wait(3)

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "😎 BUB_Scripts",
    LoadingTitle = "BUB_Scripts",
    LoadingSubtitle = "AutoCounter + NumKeyBindBot + Anti-Ragdoll + AutoClicker",
    Theme = "Default",
    ToggleUIKeybind = "K",
    DisableRayfieldPrompts = true,
    DisableBuildWarnings = true,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "BUBScripts",
        FileName = "Settings"
    },
    KeySystem = true,
    KeySettings = {
        Title = "BUB_Scripts Key System",
        Subtitle = "Verification Required",
        Note = "Join the Discord to get the key: https://discord.gg/J9BKhsn4",
        FileName = "BUBScripts_Key",
        SaveKey = false,
        GrabKeyFromSite = true,
        Key = "https://pastebin.com/raw/8dPafPL1"
    }
})

local MainTab = Window:CreateTab("Main", 4483362458)

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer

local MAX_DISTANCE = 7000          -- Increased to 7000 as requested
local TELEPORT_OFFSET = CFrame.new(0, 2, 0)

local isSpamming = false
local seatConnection = nil

-- ==================== SEAT CACHE ====================
local seatCache = {}

local function refreshSeatCache()
    seatCache = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("VehicleSeat") or obj:IsA("Seat") then
            table.insert(seatCache, obj)
        end
    end
end

workspace.DescendantAdded:Connect(function(obj)
    if obj:IsA("VehicleSeat") or obj:IsA("Seat") then
        table.insert(seatCache, obj)
    end
end)

workspace.DescendantRemoving:Connect(function(obj)
    for i = #seatCache, 1, -1 do
        if seatCache[i] == obj then
            table.remove(seatCache, i)
            break
        end
    end
end)

-- ==================== HELPER FUNCTIONS ====================
local function isProperlySeated()
    local character = player.Character
    if not character then return false end
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return false end
    
    if humanoid.SeatPart and humanoid.SeatPart.Occupant == humanoid then
        return true
    end
    return false
end

local function getNearestSeat()
    local closestSeat = nil
    local shortestDistance = math.huge
    local rootPart = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return nil end

    for _, seat in ipairs(seatCache) do
        if seat and seat.Parent and not seat.Occupant then
            local distance = (seat.Position - rootPart.Position).Magnitude
            if distance < shortestDistance and distance <= MAX_DISTANCE then
                shortestDistance = distance
                closestSeat = seat
            end
        end
    end
    return closestSeat
end

local function teleportToSeat(seat)
    if not seat or not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return end
    local rootPart = player.Character.HumanoidRootPart
    rootPart.CFrame = seat.CFrame * TELEPORT_OFFSET
    task.wait(0.03)
    local humanoid = player.Character:FindFirstChild("Humanoid")
    if humanoid and seat and not seat.Occupant then
        seat:Sit(humanoid)
    end
end

-- NEW: Gentle 15-stud lift instead of teleporting to map center
local function resetPosition()
    local rootPart = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if rootPart then
        -- Lift straight up 15 studs (much less likely to cause yanking)
        rootPart.CFrame = rootPart.CFrame + Vector3.new(0, 15, 0)
    end
end

-- ==================== MAIN AUTO COUNTER ====================
local function startSeatSpam()
    if seatConnection then return end
    refreshSeatCache()
    isSpamming = true

    seatConnection = RunService.RenderStepped:Connect(function()
        if not isSpamming then return end

        -- Only run when seat weld is broken
        if not isProperlySeated() then
            local nearest = getNearestSeat()
            if nearest then
                teleportToSeat(nearest)
            end
            resetPosition()
        end

        task.wait(0.001)
    end)
end

local function stopSeatSpam()
    isSpamming = false
    if seatConnection then
        seatConnection:Disconnect()
        seatConnection = nil
    end
end

-- ==================== ANTI RAGDOLL (Already working great) ====================
local antiRagdollEnabled = false
local unRagdollRemote = game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("unRagdoll")

RunService.Heartbeat:Connect(function()
    if not antiRagdollEnabled then return end
    local character = player.Character
    if not character then return end
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end

    if humanoid.PlatformStand or humanoid:GetState() == Enum.HumanoidStateType.Physics then
        unRagdollRemote:FireServer()
        humanoid.PlatformStand = false
    end
end)

-- ==================== CHARACTER / DEATH HANDLING ====================
local function setupCharacter(character)
    local humanoid = character:WaitForChild("Humanoid")
    humanoid.Died:Connect(function()
        task.wait(2)
        if isSpamming then
            local newChar = player.Character or player.CharacterAdded:Wait()
            setupCharacter(newChar)
            stopSeatSpam()
            task.wait(0.5)
            startSeatSpam()
            Rayfield:Notify({Title = "Auto Counter", Content = "Respawned - Restarted", Duration = 3})
        end
    end)
end

if player.Character then setupCharacter(player.Character) end
player.CharacterAdded:Connect(setupCharacter)

-- ==================== NUMPAD CYCLE ====================
local autoCycleEnabled = false
local tapsPerSecond = 10
local selectedKeys = {"KeypadOne","KeypadTwo","KeypadThree","KeypadFour","KeypadFive",
                      "KeypadSix","KeypadSeven","KeypadEight","KeypadNine","KeypadZero"}
local currentIndex = 1

task.spawn(function()
    while true do
        if autoCycleEnabled then
            local vim = VirtualInputManager
            local keyName = selectedKeys[currentIndex]
            local keyCode = Enum.KeyCode[keyName]
            
            vim:SendKeyEvent(true, keyCode, false, game)
            task.wait(0.001)
            vim:SendKeyEvent(false, keyCode, false, game)
            
            currentIndex = (currentIndex % #selectedKeys) + 1
            task.wait(1 / tapsPerSecond)
        else
            task.wait(0.1)
        end
    end
end)

-- ==================== AUTO CLICKER ====================
local autoClickerEnabled = false
local clicksPerSecond = 12

task.spawn(function()
    while true do
        if autoClickerEnabled then
            VirtualInputManager:SendMouseButtonEvent(160, 0, 0, true, game, 0)
            task.wait(0.001)
            VirtualInputManager:SendMouseButtonEvent(160, 0, 0, false, game, 0)
            task.wait(1 / clicksPerSecond)
        else
            task.wait(0.1)
        end
    end
end)

-- ==================== UI ====================
MainTab:CreateToggle({
    Name = "Auto Car Counter",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            startSeatSpam()
            Rayfield:Notify({Title = "Auto Counter", Content = "ENABLED - Smart Weld Check + 15-stud lift", Duration = 4})
        else
            stopSeatSpam()
            Rayfield:Notify({Title = "Auto Counter", Content = "DISABLED", Duration = 3})
        end
    end,
})

MainTab:CreateToggle({
    Name = "0-9 Numpad Cycle",
    CurrentValue = false,
    Callback = function(Value)
        autoCycleEnabled = Value
        Rayfield:Notify({Title = "Numpad Cycle", Content = Value and "ENABLED" or "DISABLED", Duration = 3})
    end,
})

MainTab:CreateSlider({
    Name = "Numpad Speed (Taps/sec)",
    Range = {1, 50},
    Increment = 1,
    CurrentValue = 10,
    Callback = function(Value) tapsPerSecond = Value end,
})

MainTab:CreateToggle({
    Name = "Anti-Ragdoll",
    CurrentValue = false,
    Callback = function(Value)
        antiRagdollEnabled = Value
        Rayfield:Notify({Title = "Anti-Ragdoll", Content = Value and "ENABLED" or "DISABLED", Duration = 3})
    end,
})

MainTab:CreateToggle({
    Name = "Auto Clicker",
    CurrentValue = false,
    Callback = function(Value)
        autoClickerEnabled = Value
        Rayfield:Notify({Title = "Auto Clicker", Content = Value and "ENABLED" or "DISABLED", Duration = 3})
    end,
})

MainTab:CreateSlider({
    Name = "Click Speed (Clicks/sec)",
    Range = {1, 30},
    Increment = 1,
    CurrentValue = 12,
    Callback = function(Value) clicksPerSecond = Value end,
})

Rayfield:Notify({
    Title = "BUB_Scripts Updated",
    Content = "7000 stud range + 15-stud lift (should reduce yanking)",
    Duration = 6,
})

print("✅ BUB_Scripts loaded successfully!")
